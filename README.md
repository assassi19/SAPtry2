quiz folder contains REACT FILES and handles {front - End }
where as the kahoot-backEnd has backend files and packages, 
currently I need to fix bugs, specially make sure back-end and front-end communicate properly and redo the CSS-styling and leaderboard
